__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
yt-dlp

# the Android build tree; android/.gitignore covers the rest
android/.gradle/
android/build/
android/app/build/
android/local.properties

# the iPhone build isn't ready yet, so it stays local for now
ios/
build-ipa

# Syncthing conflict copies
*.sync-conflict-*

# local-only working folders
.stignore

# the intro video is a GitHub user attachment that the README embeds as a
# player; kept out of the repository so its 6 MB isn't in every clone forever
screenshots/intro.mp4
