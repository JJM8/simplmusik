__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
yt-dlp

# the Android build tree; android/.gitignore covers the rest
android/.gradle/
android/build/
android/app/build/
android/local.properties

# the iOS build tree. `app/` is staged out of the repository root and
# `Python.xcframework` is fetched, so neither is a thing to keep here.
ios/.build/
ios/xtool/
ios/app/
ios/Python.xcframework/

# Syncthing conflict copies
*.sync-conflict-*

# local-only working folders
.stignore

# the intro video lives on the v0.2.5 release, where the README links it;
# kept out of the repository so its 6 MB isn't in every clone forever
screenshots/intro.mp4
